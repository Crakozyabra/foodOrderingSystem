package org.example.model.drink;

import org.example.model.menu.AbstractMenuItem;

public class Drink extends AbstractMenuItem {
    public Drink(Integer id, String name, Double price) {
        super(id, name, price);
    }
}
